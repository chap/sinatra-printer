apiVersion: batch/v1
kind: Job
metadata:
  name: sinatra-printer
spec:
  template:
    spec:
      containers:
      - image: ghcr.io/chap/sinatra-printer:latest
        name: sinatra-printer
        command: ["bundle", "exec", "ruby", "worker.rb"]
